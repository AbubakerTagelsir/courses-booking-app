import 'package:flutter/material.dart';

//TODO: build empty homepage widget

class Homepage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
        body: Container(
      padding: EdgeInsets.all(10),
      child: Center(
        child: Text("Homepage"),
      ),
    ));
  }
}
