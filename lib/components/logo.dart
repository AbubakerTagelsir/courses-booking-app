import 'package:flutter/material.dart';

class OurLogo extends StatelessWidget {
  const OurLogo({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Logo"),
    );
  }
}
